<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>ICELL Login</title>
    <link rel="shortcut icon" href="{{ asset('images/logo1x.png') }}" />

    {{-- Bootsrap Icons --}}
    <link rel="stylesheet" href="{{ asset('css/bootstrap-icons.css') }}">

    <!-- CSS -->
    <link rel="stylesheet" href="{{ asset('css/bootstrap1x.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/style2x.css') }}">

    {{-- Java Script --}}
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="{{ asset('js/bootstrap1x.js') }}"></script>
    <script src="{{ asset('js/login1x.js') }}"></script>

</head>

<body class="login-page">
    <section class="login-section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-8 text-center mb-3">
                    <div class="login-img">
                        <img src="{{ asset('images/logo1x.png') }}">
                        <img src="{{ asset('images/logo2x.png') }}">
                    </div>
                    <div class="login-header">
                        <h2>ICELL</h2>
                        <h4>Informasi Cepat Penyidikan Laka Lantas</h4>
                    </div>
                </div>
            </div>
            <div class="row justify-content-center">
                <div class="col-lg-5 col-md-6 text-center mb-5">
                    <div class="login-wrap">
                        <hr>
                        <h5>Selamat Datang</h5>
                        <small class="">Silahkan Masukkan Nama Pengguna dan Kata Sandi</small>
                        @if ($errors->any())
                            <div class="card border-danger">
                                <div class="card-body text-danger text-center">
                                    @foreach ($errors->all() as $error)
                                        <span class="fw-bold">{{ $error }}</span>
                                    @endforeach
                                </div>
                            </div>
                        @endif


                        <form class="mt-3" method="POST" action="{{ route('authenticate') }}" autocomplete="off">
                            @csrf
                            <div class="mb-3">
                                <input class="forms-control @error('username') is-invalid @enderror" type="text"
                                    name="username" id="username" autocomplete="username" required
                                    placeholder="Silahkan Masukkan Nama Pengguna">
                                @error('username')
                                    <span class="invalid-feedback" role="alert">
                                        <strong>{{ $message }}</strong>
                                    </span>
                                @enderror
                            </div>
                            <div class="password-container mb-3">
                                <input class="forms-control @error('password') is-invalid @enderror" type="password"
                                    name="password" id="password" placeholder="Silahkan Masukkan Kata Sandi"
                                    autocomplete="current-password">
                                <span toggle="#togglePassword" class="bi bi-eye field-icon toggle-password"
                                    id="togglePassword"></span>
                                @error('password')
                                    <span class="invalid-feedback" role="alert">
                                        <strong>{{ $message }}</strong>
                                    </span>
                                @enderror
                                <a class="text-start forgot-password" href="{{ route('forget-password') }}">Lupa Kata Sandi?
                                    <strong class="ms-1">Klik Disini</strong>
                                </a>
                            </div>
                            <hr>
                            <div class="mb-3 d-flex">
                                <div class="captcha col-7">
                                    <span class="img-captcha me-1">{!! Captcha::img() !!}</span>
                                    <button type="button" class="btn reload btn-secondary" id="reload"
                                        data-refresh-url="{{ route('refresh_captcha') }}">
                                        <i class="bi bi-arrow-clockwise fw-bold"></i>
                                    </button>
                                </div>
                                <div class="col-5">
                                    <input id="captcha" type="text" class="forms-control input-captcha"
                                        placeholder="Masukkan Captcha" name="captcha">
                                </div>
                            </div>
                            <hr>
                            <div class="d-grid gap-2">
                                <button type="submit" class="btn btn-login">Masuk</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
</body>

</html>


{{-- <div class="d-lg-flex container-fluid">
        <div class="row vh-100">
            <div class="col-6 p-0">
                <img class="bg-image" src="{{ asset('images/background-image.jpg') }}" alt="">
            </div>
            <div class="col-6 p-0">
                <div class="card px-4 py-5 rounded-4">
                    <div class="row mb-2">
                        <div class="col-6"><img class="img-sm float-end" src="{{ asset('images/logo1.png') }}"></div>
                        <div class="col-6"><img class="img-sm mt-1" src="{{ asset('images/logo2.png') }}"></div>
                    </div>
                    <div class="text-center mb-3">
                        <h2 class="fw-bolder">ICELL</h2>
                        <h5>Informasi Cepat Penyidikan Laka Lantas</h5>
                    </div>

                    <div class="text-center border-bottom border-secondary border-2 pb-2 mb-3">
                        <span class="fs-5 fw-semibold">Selamat Datang</span> <br>
                        <span class="fs-6 fw-bold text-body-tertiary">Silahkan Masukkan Nama Pengguna dan Kata
                            Sandi</span>
                    </div>
                    @if ($errors->any())
                        <div class="card border-danger">
                            <div class="card-body text-danger text-center">
                                @foreach ($errors->all() as $error)
                                    <span class="fw-bold">{{ $error }}</span>
                                @endforeach
                            </div>
                        </div>
                    @endif

                    <form method="POST" action="{{ route('authenticate') }}" autocomplete="off">
                        @csrf
                        <div class="form-group mb-2">
                            <label>
                                <h6 class="fw-bold">Nama Pengguna</h6>
                            </label>
                            <input class="form-control @error('username') is-invalid @enderror" type="text"
                                name="username" id="username" autocomplete="username" required
                                placeholder="Silahkan Masukkan Nama Pengguna">
                            @error('username')
                                <span class="invalid-feedback" role="alert">
                                    <strong>{{ $message }}</strong>
                                </span>
                            @enderror
                        </div>
                        <div class="form-group mb-3">
                            <label>
                                <h6 class="fw-bold">Kata Sandi</h6>
                            </label>
                            <div class="input-group mb-1">
                                <input class="form-control @error('password') is-invalid @enderror" type="password"
                                    name="password" id="password" placeholder="Silahkan Masukkan Kata Sandi"
                                    autocomplete="current-password">
                                <span class="input-group-text">
                                    <i class="bi bi-eye" id="togglePassword" style="cursor: pointer"></i>
                                </span>
                            </div>
                            @error('password')
                                <span class="invalid-feedback" role="alert">
                                    <strong>{{ $message }}</strong>
                                </span>
                            @enderror
                            <a class="fw-bold fs-6 forgot-password" href="">Lupa Kata Sandi</a>
                        </div>
                        <div class="row mb-3">
                            <div class="col-lg-4 col-md-4 col-sm-12 col-12 pe-0 mb-3">
                                <div class="captcha">
                                    <span class="img-captcha me-2">{!! Captcha::img() !!}</span>
                                    <button type="button" class="btn reload btn-secondary" id="reload"
                                        data-refresh-url="{{ route('refresh_captcha') }}">
                                        <i class="bi bi-arrow-clockwise fw-bold"></i>
                                    </button>
                                </div>
                            </div>
                            <div class="col-lg-8 col-md-8 col-sm-12 col-12">
                                <input id="captcha" type="text" class="form-control input-captcha"
                                    placeholder="Masukkan Hasil Penjumlahan Captcha" name="captcha">
                            </div>
                            @if ($errors->has('captcha'))
                                <span class="help-block text-white">
                                    <strong>{{ $errors->first('captcha') }}</strong>
                                </span>
                            @endif
                        </div>
                        <div>
                            <button type="submit" class="col-12 btn btn-dark-blue text-white fw-bold">Masuk</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div> --}}
